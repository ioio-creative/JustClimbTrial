<Window x:Class="JustClimbTrial.Views.Windows.Playground"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JustClimbTrial.Views.Windows"
        mc:Ignorable="d"
        Title="Playground" Height="720" Width="1280">
    <Grid>
        <Image Name="playgroundCamera"></Image>
        <Canvas Name="playgroundCanvas" Width= "{Binding ActualWidth, ElementName=playgroundCamera, Mode=OneWay}" Height = "{Binding ActualHeight, ElementName=playgroundCamera, Mode=OneWay}"></Canvas>
        <MediaElement Name="playgroundPlayback"
                      Width="{Binding RelativeSource={RelativeSource Self},Path=Parent.ActualWidth}"
                      Height="{Binding RelativeSource={RelativeSource Self},Path=Parent.ActualHeight}" />
    </Grid>
</Window>
